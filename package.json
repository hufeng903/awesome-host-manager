{
  "name": "awesome-hosts-manager",
  "version": "1.0.16",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-redux": "^8.1.3",
    "react-scripts": "5.0.1",
    "redux": "^4.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "clean": "rm -rf awesome-host-manager",
    "build": "npm test && npm run clean && react-scripts build && npm run rename",
    "rename": "mv build awesome-host-manager",
    "test": "react-scripts test --env=jsdom --watchAll=false",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
